export enum State {
    LOADING,
    START,
    GAME,
    SETTING
}

export enum EventCall {
    MOVE,
    SHOOT,
    ATTACK,
    USE_SKILL,
    STOP,
    SHOW
}

export enum EventMove {
    UP,
    DOWN,
    LEFT,
    RIGHT
}